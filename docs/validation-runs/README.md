# Validation runs

This directory stores generated evidence reports from real ANBE validation
runs.

Generate a report with:

```bash
scripts/collect-validation-evidence.sh --help
```

Reports should be generated from actual build artifacts and, where useful,
from captured runtime logs on physical Android devices.
